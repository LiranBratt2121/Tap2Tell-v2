import styled from "styled-components";
import { SendButton } from "./SendButton";

export const BackButton = styled(SendButton)`
    position: absolute;
    top: 1rem;
    left: 1rem;

    width: auto;
    height: auto;
`
